Каждое задание варианта должно быть в отдельном файле исходного кода, входные значения должны передаваться через аргументы, используя модуль sys и переменной argv. Выходные значения (ответы) во всех заданиях варианта необходимо выводить на консоль

1. Даны три целых числа: A, B, C. Проверить истинность высказывания: «Ровно одно из чисел A, B, C положительное».
2. Дано целое положительное трехзначное число. В нем зачеркнули первую справа цифру и приписали ее слева. Вывести полученное новое число.
3. Дан целочисленный массив, состоящий из N элементов (N > 0, N может быть четным или нечетным числом). Поменять порядок следования его элементов на обратный. Вычислить и вывести сумму и произведение всех его элементов.
4. Вводится строка, содержащая латинские буквы и круглые скобки. Длина строки может быть разной. Если скобки расставлены правильно (т. е. каждой открывающей скобки соответствует одна закрывающая скобка), то вывести число 0. В противном случае вывести или номер позиции, в которой расположена первая ошибочная закрывающая скобка, или, если закрывающих скобок не хватает, значение −1.
5. Вводится строка, состоящая из слов, разделенных подчеркиваниями (одним или несколькими). Требуется удалить из этой строки повторяющиеся символы. Например, если было введено "abc_cde_defg", то должно быть выведено "abcdefg".
import sys

def main():
    if len(sys.argv) != 4:
        print("Ошибка: необходимо ввести три числа.")
        print("Пример: python task1.py 5 -2 10")
        return
    
    try:
        a, b, c = map(int, sys.argv[1:4])
    except ValueError:
        print("Ошибка: все аргументы должны быть целыми числами.")
        return
    
    # Подсчет количества положительных чисел
    positive_count = 0
    if a > 0:
        positive_count += 1
    if b > 0:
        positive_count += 1
    if c > 0:
        positive_count += 1
    
    # Проверка: ровно одно положительное
    result = positive_count == 1
    print(result)

if __name__ == "__main__":
    main()


task2

import sys

def main():
    if len(sys.argv) != 2:
        print("Ошибка: необходимо ввести одно трехзначное число.")
        print("Пример: python task2.py 123")
        return
    
    try:
        num = int(sys.argv[1])
    except ValueError:
        print("Ошибка: аргумент должен быть целым числом.")
        return
    
    # Проверка, что число трехзначное и положительное
    if num < 100 or num > 999:
        print("Ошибка: число должно быть трехзначным (100-999).")
        return
    
    # Первую цифру справа (единицы) переносим влево
    units = num % 10
    rest = num // 10
    new_num = units * 100 + rest
    
    print(new_num)

if __name__ == "__main__":
    main()

task3

import sys

def main():
    if len(sys.argv) < 2:
        print("Ошибка: необходимо ввести размер массива и его элементы.")
        print("Пример: python task3.py 5 1 2 3 4 5")
        return
    
    try:
        n = int(sys.argv[1])
        if n <= 0:
            print("Ошибка: размер массива N должен быть > 0.")
            return
        
        if len(sys.argv) != n + 2:
            print(f"Ошибка: ожидается {n} элементов, но получено {len(sys.argv) - 2}.")
            return
        
        arr = list(map(int, sys.argv[2:2+n]))
    except ValueError:
        print("Ошибка: все элементы должны быть целыми числами.")
        return
    
    # Разворот массива
    reversed_arr = arr[::-1]
    
    # Сумма и произведение
    total_sum = sum(arr)
    total_product = 1
    for num in arr:
        total_product *= num
    
    print("Развернутый массив:", reversed_arr)
    print("Сумма элементов:", total_sum)
    print("Произведение элементов:", total_product)

if __name__ == "__main__":
    main()

task4 

import sys

def main():
    if len(sys.argv) != 2:
        print("Ошибка: необходимо ввести строку в кавычках.")
        print("Пример: python task4.py \"(a+b)*(c-d)\"")
        return
    
    s = sys.argv[1]
    stack = []
    
    for i, char in enumerate(s, start=1):  # позиции начинаем с 1
        if char == '(':
            stack.append(i)
        elif char == ')':
            if not stack:
                # Найдена закрывающая скобка без открывающей
                print(i)
                return
            else:
                stack.pop()
    
    if stack:
        # Остались открывающие скобки без закрывающих
        print(-1)
    else:
        print(0)

if __name__ == "__main__":
    main()

task5

import sys

def main():
    if len(sys.argv) != 2:
        print("Ошибка: необходимо ввести строку в кавычках.")
        print("Пример: python task5.py \"abc_cde_defg\"")
        return
    
    s = sys.argv[1]
    result = ""
    seen = set()
    
    for char in s:
        if char not in seen:
            result += char
            seen.add(char)
    
    print(result)

if __name__ == "__main__":
    main()
